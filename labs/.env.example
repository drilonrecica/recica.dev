# Preview builds are noindex unless production explicitly enables indexing.
PUBLIC_INDEXING_ENABLED=false

# Optional only for loopback E2E/local canonical tests.
# PUBLIC_SITE_URL=http://127.0.0.1:4175
